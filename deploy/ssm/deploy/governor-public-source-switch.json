{
  "InstanceIds": ["i-06edd717a43f763b7"],
  "DocumentName": "AWS-RunShellScript",
  "Comment": "deploy governor public source switch: promote hook + monitor API + env prefixes",
  "Parameters": {
    "commands": [
      "set -e",
      "TS=governor-public-source-20260624",
      "BUCKET=ch7-static-bkkelection2569",
      "PREFIX=s3://${BUCKET}/tmp/deploy",
      "install -d -o ec2-user -g ec2-user /opt/election/hermes/governor_results",
      "aws s3 cp ${PREFIX}/hermes-governor-results-public-source-${TS}.py /opt/election/hermes/governor_results/public_source.py",
      "aws s3 cp ${PREFIX}/hermes-governor-results-init-${TS}.py /opt/election/hermes/governor_results/__init__.py",
      "aws s3 cp ${PREFIX}/hermes-static-results-export-${TS}.py /opt/election/hermes/supervisor/services/static_results_export.py",
      "aws s3 cp ${PREFIX}/hermes-supervisor-intake-server-${TS}.py /opt/election/hermes/supervisor/services/intake_server.py",
      "aws s3 cp ${PREFIX}/hermes-results-api-app-${TS}.py /opt/election/hermes/results_api/app.py",
      "chown -R ec2-user:ec2-user /opt/election/hermes/governor_results /opt/election/hermes/supervisor/services/static_results_export.py /opt/election/hermes/supervisor/services/intake_server.py /opt/election/hermes/results_api/app.py",
      "sudo -u ec2-user bash -lc 'cd /opt/election && chmod +x deploy/ec2/deploy.sh && ./deploy/ec2/deploy.sh'",
      "sudo -u ec2-user bash -lc 'cd /opt/election && python3 - <<\"PY\"\nfrom pathlib import Path\nimport re\n\npath = Path(\".env\")\ntext = path.read_text(encoding=\"utf-8\")\nupdates = {\n    \"GOVERNOR_RESULTS_PREFIX\": \"api-data/governor-results\",\n    \"RESULTS_API_STATIC_RESULTS_PREFIX\": \"api-data/governor-results\",\n    \"STATIC_RESULTS_PREFIX\": \"api-data/governor-results-dev\",\n}\nfor key, value in updates.items():\n    line = f\"{key}={value}\"\n    if re.search(rf\"^{re.escape(key)}=.*$\", text, flags=re.M):\n        text = re.sub(rf\"^{re.escape(key)}=.*$\", line, text, flags=re.M)\n    else:\n        if not text.endswith(\"\\n\"):\n            text += \"\\n\"\n        text += line + \"\\n\"\npath.write_text(text, encoding=\"utf-8\")\nfor key in updates:\n    print(re.search(rf\"^{re.escape(key)}=.*$\", text, flags=re.M).group(0))\nPY'",
      "sudo -u ec2-user bash -lc 'cd /opt/election && docker compose --env-file .env up -d --no-deps --build results-api line-relay && docker compose --env-file .env restart hermes-supervisor'",
      "sudo -u ec2-user bash -lc 'cd /opt/election && for i in $(seq 1 30); do status=$(docker inspect --format \"{{if .State.Health}}{{.State.Health.Status}}{{else}}{{.State.Status}}{{end}}\" election-results-api-1); echo results-api=$status; if [ \"$status\" = \"healthy\" ] || [ \"$status\" = \"running\" ]; then break; fi; sleep 2; done'",
      "sudo -u ec2-user bash -lc 'cd /opt/election && docker compose exec -T line-relay python - <<\"PY\"\nfrom hermes.supervisor.services.static_results_export import export_static_governor_results\nprint(export_static_governor_results())\nPY'",
      "sudo -u ec2-user bash -lc 'cd /opt/election && docker compose exec -T results-api python - <<\"PY\"\nfrom hermes.results_api.app import effective_active_public_source_config\nprint(effective_active_public_source_config())\nPY'",
      "sudo -u ec2-user bash -lc 'cd /opt/election && echo ---S3--- && aws s3 ls s3://ch7-static-bkkelection2569/api-data/governor-results/ && aws s3 ls s3://ch7-static-bkkelection2569/api-data/governor-results-dev/'"
    ]
  }
}
